#include <stdio.h>
#include <math.h>
// Circumference of Circle
int main() {
    float r;
    printf("Enter the Radius of the Circle: ");
    scanf("%f", &r);
    printf("The Circumference of the Circle is %f", M_PI*2*r);
}
